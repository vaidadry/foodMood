package vaida.dryzaite.foodmood.utilities
/*
import android.app.Application
import android.content.Context
import android.content.res.Resources
import androidx.test.core.app.ApplicationProvider
import androidx.test.ext.junit.runners.AndroidJUnit4
import org.hamcrest.MatcherAssert.assertThat
import org.hamcrest.core.Is.`is`
import org.junit.Test
import org.junit.runner.RunWith
import org.mockito.Mock
import org.mockito.Mockito.`when`
import org.mockito.junit.MockitoJUnitRunner
import vaida.dryzaite.foodmood.R

@RunWith(MockitoJUnitRunner::class)
class ConverterTests {

    @Mock
    private lateinit var mockContext: Context

// hOW to get resources?

    @Test
    fun convertNumericMealTypeToString_1_returnsBreakfast() {
        //GIVEN
        val mealType = 1
        `when`(mockContext.resources.getString(R.string.breakfast))
            .thenReturn("Breakfast")

        //WHEN
        val mealString = convertNumericMealTypeToString(1, mockContext.resources)
        //resources

        //THEN
        assertThat(mealString, `is`("Breakfast"))
    }
}
 */